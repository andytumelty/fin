<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= "fin" %></title>

    <%= stylesheet_link_tag "application" %>
    <%= csrf_meta_tags %>
  </head>

  <body>

  <header class="navigation">
      <%= link_to image_tag("logo.png", alt: "logo"), root_url, class: "logo" %>
      <div class="nav">
        <ul id="navigation-menu">
          <% if current_user %>
            <li class="nav-link"><%= link_to "Transactions" , transactions_path   %></li>
            <li class="nav-link"><%= link_to "Accounts"     , accounts_path       %></li>
            <li class="nav-link"><%= link_to "Categories"   , categories_path     %></li>
            <li class="nav-link"><%= link_to "Budgets"      , latest_budget_path  %></li>
          <% end %>
        </ul>
      </div>
      <div id="user-menu">
        <% if current_user %>
          <%= link_to button_tag("Edit Profile"), edit_user_path(current_user.id) %>
          <%= link_to button_tag("Logout"), logout_path, method: :post %>
        <% else %>
          <%= link_to button_tag("Register"), new_user_path %>
          <%= link_to button_tag("Login"), login_path %>
        <% end %>
      </div>
    </div>
  </header>

    <h1>Fin</h1>

    <div class="notice"><%= flash[:notice] %></div>
    <div class="alert"><%= flash[:alert] %></div>

    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>