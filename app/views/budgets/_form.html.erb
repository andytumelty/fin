<div style="width:300px;margin:0 auto">
<%= form_for(@budget) do |f| %>
  <% if @budget.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@budget.errors.count, "error") %> prohibited this budget from being saved:</h2>

      <ul>
      <% @budget.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :name, placeholder: 'Name (optional)' %>
  </div>
  <div class="field">
    <%= f.text_field :start_date, placeholder: 'Start Date', class: 'datepicker' %>
  </div>
  <div class="field">
    <%= f.text_field :end_date, placeholder: 'End Date', class: 'datepicker' %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
</div>